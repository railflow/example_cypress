{
  "name": "railflow-cypress-reporter-test-project",
  "version": "1.0.0",
  "description": "Usage examples for railflow-cypress-reporter",
  "main": "src/app.ts",
  "scripts": {
    "delete:reports": "rm cypress/results/* || true",
    "precypress": "npm run delete:reports",
    "cypress": "cypress run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.1",
    "eslint": "^7.0.0",
    "eslint-plugin-cypress": "^2.11.3",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "cypress": "10.10.0",
    "railflow-cypress-junit-reporter": "1.0.2"
  },
  "eslintConfig": {
    "extends": [
      "plugin:cypress/recommended"
    ],
    "globals": {
      "Cypress": true
    }
  }
}
